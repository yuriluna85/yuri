# ... (partes anteriores iguais) ...
      - name: Salvar e commitar alterações
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          git add mencoes.csv index.html
          git diff --staged --quiet || git commit -m "🤖 [Auto] Atualiza Painel de Menções HTML"
          git push
